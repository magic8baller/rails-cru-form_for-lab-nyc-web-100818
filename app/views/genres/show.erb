
<h1><%= @genre.name %></h1>
<%= link_to 'Disagree?? EDIT ME', edit_genre_path(@genre) %>
<h2>Relevant Songs:</h2>
<ul>
  <% @genre.songs.each do |s| %>
    <li>
      <%= link_to "#{s.name}", s %>
    </li>
  <% end %>
</ul>
