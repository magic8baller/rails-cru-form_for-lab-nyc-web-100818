<h1>Add A New Artist!</h1>

<%= form_for @artist, url: artists_path do |f| %>

  <%= f.label :name, 'Name:'  %>
  <%= f.text_field :name %>

  <%= f.label :bio, 'Bio: '  %>
  <%= f.text_field :bio %>
<br>
  <%= f.submit %>
  <% end %>
